Important Security Notes
Privileges Required: This script must be run with administrative privileges:

Linux: sudo or as root user

Windows: As Administrator

Password Security:

Passwords provided as command-line arguments may be visible in process lists

Use interactive mode for sensitive passwords when possible

Consider using password vaults in production environments

Platform Support:

L
